/*
 * This is the main function is uesd to call other functions
 * This is the code when study c program tutorial by using xcode as IDE
 * All functions are declared in functions.h file
 */

#include <stdio.h>
#include "functions.h"

int main(int argc, char *argv[]) {
    /*
     * return helloworld();  // helloworld.c
     * return standardin();  // standardin.c
     * integerTypes(); // intergerTypes.cd
     * floatPointTypes(); // floatPointTypes.c
     */
    boolTpes();
    return 0;
}
